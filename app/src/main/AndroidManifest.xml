<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.makerenew">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Makerenew">
        <activity
            android:name=".glucose_plus"
            android:exported="false" />
        <activity
            android:name=".glucose_bluetooth"
            android:exported="false" />
        <activity
            android:name=".glucose_graph"
            android:exported="false" />
        <activity
            android:name=".glucose_calander"
            android:exported="false" />
        <activity
            android:name=".Glucose"
            android:exported="false" />
        <activity
            android:name=".Signup"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".Splash" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".Splash"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Splash Activity -->
        <activity android:name=".MainActivity" />
        <activity android:name=".Login_activity" />
        <activity android:name=".MainActivity2" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>